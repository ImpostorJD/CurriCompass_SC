import { CourseFilterPipe } from './course-pipe.pipe';

describe('CoursePipePipe', () => {
  it('create an instance', () => {
    const pipe = new CourseFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
