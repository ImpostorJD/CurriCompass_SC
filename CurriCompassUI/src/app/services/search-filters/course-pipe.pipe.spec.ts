import { CoursePipePipe } from './course-pipe.pipe';

describe('CoursePipePipe', () => {
  it('create an instance', () => {
    const pipe = new CoursePipePipe();
    expect(pipe).toBeTruthy();
  });
});
