import { CourseAvailableFilterPipe } from './course-available-filter.pipe';

describe('CourseAvailableFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CourseAvailableFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
