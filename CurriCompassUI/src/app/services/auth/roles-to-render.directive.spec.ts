import { RolesToRenderDirective } from './roles-to-render.directive';

describe('RolesToRenderDirective', () => {
  it('should create an instance', () => {
    const directive = new RolesToRenderDirective();
    expect(directive).toBeTruthy();
  });
});
